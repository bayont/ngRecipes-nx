import { TextStylePipe } from './text-style.pipe';

describe('TextStylePipe', () => {
  it('create an instance', () => {
    const pipe = new TextStylePipe();
    expect(pipe).toBeTruthy();
  });
});
